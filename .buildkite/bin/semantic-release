#!/bin/sh


set -eu

SCRIPT_DIR=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )
source ${SCRIPT_DIR}/common

docker_run "chown -R \$(id -gn):\$(whoami) . 2>/dev/null && yarn install && GITHUB_TOKEN=$GITHUB_API_TOKEN npx --no-install semantic-release -t 'v\${version}'"